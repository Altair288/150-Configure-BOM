<core:FragmentDefinition xmlns:core="sap.ui.core" xmlns="sap.m">
  <Dialog id="configurationDialog" title="{i18n>confirmConfiguration}">
    <content>
      <Text text="{i18n>confirmConfigurationText}" class="sapUiContentPadding" />
    </content>
    <beginButton>
      <Button text="{i18n>confirm}" type="Emphasized" />
    </beginButton>
    <endButton>
      <Button text="{i18n>cancel}" />
    </endButton>
  </Dialog>
</core:FragmentDefinition>